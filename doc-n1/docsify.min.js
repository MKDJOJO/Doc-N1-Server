< / \\ 1>[^ \\ n] * \\ n + | $）|评论[^ \\ n] *（\\ n + | $）| <\\？[\\ s \\ S] *？ \\？>\\ n * | <！[AZ][\\ s \\ S] *？>\\ n * | <！\\[CDATA \\[[\\ s \\ S] *？\ \] \\]>\\ n * |</？（
tag）（ ? : + | \\n | /？>）[\\ s \\ S] *？（？：\\ n {
2，
}
| $）| <（?!script | pre | style）（[az][\\ w -] *）（?: attribute）*？* /？ > （？ = [\\t] * （？：\\n | $））[\\s\\ S] * ？（？：\\n {
2，
} | $） | < /（ ？！script | pre | style）[az][\\ w-] * \\ s *>（？=[\\ t] *（？：\\ n | $））[\\ s \\ S] *？（？：\\ n {
2，
}
| $））“，def：/ ^ {
0, 3
}\[（label）\]： * \n？ * < ？（[ ^ \s > ] + ） > ？（？: ( ? : +\n？ * | * \n * ）（title））？ * （？：\n + | $） / ，nptable： h， table： h， lheading： / ^ （[ ^ \n] + ）\n {
  0, 3
} （ = + | -+） * （？：\n + | $） / ，_ paragraph： / ^ （[ ^ \n] + （？：\n（ ?!hr | head | heading | blockquote | fences | list | html）[ ^ \n] + ） * ）/，text：/ ^ [ ^ \n] + /
}
;
函数l（e） {
  this.tokens = [] ，this.tokens.links = Object.create（null），this.options = e || m.defaults，this.rules = y.normal，this.options.pedantic？this.rules = y.pedantic：this.options.gfm &&（此。_comment）.replace（/
  tag / g，“（？！（？ : (a： em | strong | small | s | cite | q | dfn | abbr | data | time | code |
var | samp | kbd | sub | sup | i | b | u | mark | ruby | rt | rp | bdi | bdo | span | br | wbr | ins | del | img）\\ b）\\ w + （？！： | [ ^ \\w\\ s@] * @）\\b“​​）。 getRegex（）， def： / ^ * \[（
  [ ^ \]
]+ ）\]： * < ？（[ ^ \s > ] + ） > ？（？：+（[“（][ ^ \n]+[”）]）））？ * （？：\n + | $） / ，标题：/ ^ *（＃ {
    1, 6
  }
  ）*（[^ \ n] +？）*（？：＃+ *）？（？：\ n + | $ ）/，
  fences： h， paragraph： e（ y.normal._paragraph）.replace（“ hr”， y.hr）.replace（“ heading”，“ * ＃{
    1, 6
  } * [ ^ \n]”）.replace（“ lheading”， y.lheading）.replace（“ blockquote”，“ {
    0, 3
  } > ”）。replace（“ | fences，”“”）。 replace（“ | list”，“”）.replace（“ | html”，“”）。 getRegex（）
} ），
l.rules = y， l.lex = function（ e， t） {
  返回新的
  l（ t）.lex（ e）
} ，
l。 prototype.lex = function（ e） {
  返回
  e = e.replace（ / \r\ n | \r / g，“\ n”）。 replace（ / \t / g，不应使用，将来会删除。在此处阅读更多信息： https: //marked.js.org/#/USING_ADVANCED.md#options“）
} 函​​数
m（ e， n， r） {
  if（ null == e）引发新错误（” marked（）：输入参数未定义或为 null“）;
  if（” string“！ = typeof e）抛出新错误（” marked（）：输入参数的类型为“ + Object.prototype.toString.call（ e） + ”，预期为字符串“）;
  if（ r || ” function“ == typeof n） {
    r || （（r = n， n = null）， f（ n = d（ { } ，
    m.defaults， n || {}）））;
    var i， a， s = n.highlight， t = 0;
    try {
      i = l.lex（ e， n）
    } catch（ e） {
      return r（ e）
    }
    a = i.length; 函数
    o（ t） {
      if（ t）返回 n.highlight = s， r（ t）;
      var e;
      try {
        e = c.parse（ i， n）
      } catch（ e） {
        t = e
      } 返回
      n.highlight = s， t？ r（ t）： r（ null， e）
    }
    if（！ s || s.length < 3）返回 o（）;
    if（删除 n.highlight，！ a）返回 o（）;
    for（;
    t < i.length;
    t++）！ function（ n） {
    “
      code”！ == n.type？ - a || o（）： s（ n.text， n.lang，
      function（ e， t） {
        返回
        e？ o（ e）： codeURIComponent（ l）] = s）
      }
    }
    if（！ g） {
      var p = e.getCurrentPath（）;
      S（“ scroll”，
      function（） {
        return we（ p）
      } ），
      _（“ scroll”，
      function（） {
        return we（ p）
      } ），
      _（ n，“ mouseover”，
      function（） {
        ve = ！0
      } ），
      _（ n，“ mouseleave”，
      function（） {
        ve = ！1
      } ）
    }
  }
} （
this.router）， n && e） {
  var i = m（“＃ main”）， a = i.children[0];
  if（ a && “H1”！ == a.tagName） {
    var s = w（“ h1“）;
    s.innerText = e.innerText， x（ i， s）
  }
}
r && function（ e） {
  void 0 === e && （e = 0）， Se.scrollTop = ！0 === e？ 0： Number（ e）
} （
r）
}，
Ze._renderNav = f unction（ e） {
  e && this._renderTo（“ nav”， this.compiler.compile（ e））， this.config.loadNavbar && ce（ this.router，“ nav”）
} ，
Ze._renderMain = f unction（ r， i， a） {
  var s = t his;
  if（ void 0 === i && （i = {}）, !r）返回 Fe.call（ this， r）;
  p（此“ beforeEach”， r，函数（ e） {
    函数
    t（） {
      i.updatedAt && （n = E e（ n， i.updatedAt， s。 config.formatUpdated））， p（ s，“ afterEach”， n，
      function（ e） {
        返回
        Fe.call（ s， e）
      } ）
    }
    var n;
    s.isHTML？（ n = s.result = r， t（）， a（））： $ e（ {
      compiler： s.compiler， raw： e
    } ，
    function（ e） {
      n = s.compiler.compile（ e）， t（）， a（）
    } ）
  } ）
} ，
Ze._renderCover = f unction（ e， t） {
  var n = m（“。 cover”）;
  if（ A（ m（“ main”）， t？“ add”：“ remove”，“ hidden”）， e） {
    A（ n，“ add”，“ show”）;
    var r = t his.coverIsHTML？ e： this.compiler.cover（ e）， i = r.trim（）。 match（ '<p> <img 。*？data-origin =“（。*？）” [^ a] + alt =“（。*？）”>（[^ <] *？）</ p> $'）;
    if（ i） {
      if（“ color” === i[2]） n.style.background = i[1] + （i[3] || “”）;
            else {
        var a = i[1];
        A（ n，“添加“，”具有掩码“）， W（ i[1]） || | （a = K（ this.router.getBasePath（）， i[1]））， n.style.backgroundImage = ”url（“ + a + “）”，n.style.backgroundSize = “cover”， n.style。 backgroundPosition = “center center”
      }
      r = r.replace（ i[0]，“”）
    }
    this._renderTo（“。 cover - main”， r）， le（）
  } 其他
  A（ n，“删除”，“ show“）
} ，
Ze._updateRender = f unction（） {
！
  function（ e） {
    var t = m（”。 app - name - link“）， n = e.config.nameLink， r = e.route.path;
    if（ t）
    if（ c（ e.config.nameLink）） t.setAttribute（“ href”， n）; 否则
    if（“ object” == t ypeof n） {
      var i = O bject.keys（ n）.filter（
      function（ e） {
        return -1 < r.indexOf（ e）
      } ）[0];
      t.setAttribute（“ href”， n[i]）
    }
  } （
  this）
} ，（
Be = U e）._ loadSideAndNav = f unction（ e， t， n， r） {
  var i = t his;
  return function（） {
    if（！ n）
    return r（）;
    qe（ e， t， n，
    function（ e） {
      i._renderSidebar（ e）， r（）
    } ，
    i，！ 0）
  }
} ，
Be._fetch = f unction（ n） {
  var r = t his;
  void 0 === n && （n = h）;
  var e = t his.route， i = e。路径， a = V（ e.query，[“id”]）， t = t his.config， s = t.loadNavbar， o = t.requestHeaders， l = t.loadSidebar， c = t his.router.getFile（ i）， u = Y e（ c + a， 0， o）;
  this.isHTML = / \。html $ /
  g.test（ c）， u.then（
  function（ e， t） {
    return r._renderMain（ e， t， r._loadSideAndNav（ i， a， l， n））
  } ，
  function（ e） {
    r._fetchFallbackPage（ c， a， n） || r._fetch404（ c， a， n）
  } ），
  s && qe（ i， a， s，
  function（ e） {
    返回
    r._renderNav（ e）
  } ，
  this，！ 0）
} ，
Be._fetchCover = f unction（） {
  var t = t his， e = t his.config， n = e.coverpage， r = e.requestHeaders， i = t his.route.query， a = X（ this.route.path）;
  if（ n） {
    var s = n ull， o = t his.route.path;
    if（“ string” == t ypeof n）“ / ” === o && （s = n）;
        else if（ Array.isArray（ n）） s = -1 < n.indexOf（ o） && “_ coverpage”;
        else {
      var l = n[o];
      s = ！0 === l？“ _coverpage”： l
    }
    var c = B oolean（ s） && this.config.onlyCover;
    return s？（ s = t his.router.getFile（ a + s）， this.coverIsHTML = / \。html $ /
    g.test（ s）， R（ s + V（ i，[“id”]）, !1， r）.then（
    function（ e） {
      return t._renderCover（ e， c）
    } ）））：
    this._renderCover（ null， c）， c
  }
} ，
Be。 $ fetch = f unction（ e） {
  var t = t his; 函数
  n（） {
    p（ t，“ doneEach”）， e（）
  } 无效
  0 === e && （e = h）， this._fetchCover（）？ n（）： this._fetch（
  function（） {
    t。 $ resetEvents（）， n（）
  } ）
} ，
Be._fetchFallbackPage = f unction（ n， r， i） {
  var a = t his;
  void 0 === i && （i = h）;
  var e = t his.config， t = e.requestHeaders， s = e.fallbackLanguages， o = e.loadSidebar;
  if（！ s）
  return ！ 1;
  var l = n.split（“ / ”）[1];
  if（ -1 === s。 indexOf（ l））
  return ！ 1;
  var c = n.replace（ new RegExp（“ ^ /” + l），“”）;
  返回Ye（c + r，0，t）.then（function（e，t ） {
    返回a._renderMain（e，t，a._loadSideAndNav（n，r，o，i））
  }
  ，function（） {
    返回a._fetch404（n，r，i）
  }
  ）, !0
}
，是。 _fetch404 = f unction（e，t，n） {
  var r = t his;
  void 0 === n &&（n = h ）;
  var i = t his.config，a = i.loadSidebar，s = i.requestHeaders，o = i.notFoundPage，l = t his._loadSideAndNav（e，t，a，n）;
  if（o） {
    var c = f unction（t，e） {
      var n，r，i = e.notFoundPage，a =“ _ 404” + （e.ext ||“ .md”）;
    switch（i的类型） {
      caseboolean“：r = a;
      break;
                case ” string“：r = i;
      break;
                case ” object“：r =（n = O bject.keys（i）.sort（function（e，t） {
        return t.length - e.length
      }
      ）。find（function（e） {
        return t.match（new RegExp（“ ^” + e））
      }
      ））&& i[n] || a
    }
    返回r
  }
  （e，this.config）;
  return Ye（this.router.getFile（c），0，s）.then（function（e，t） {
    return r._renderMain（e，t，l）
  }
  ，function（） {
    return r._renderMain（null ， {
    }
    ，​​l）
  }
  ）, !0
}
返回this._renderMain（null， {
}
，l）, !1
}
，Ue。$ resetEvents = f unction（） {
  _ e（this.route.path，this.route。 query.id），this.config.loadNavbar && ce（this.router，“ nav”）
}
，window.Docsify = {
  util：He，dom：e，get：R，slugify：B，version：“ 4.10.2”
}
，window.DocsifyCompiler = s e，window.marked = j ，window.Prism = M ，function（e） {
  var t = d ocument.readyState;
  if（“ complete”=== t ||“ interactive”=== t ）返回setTimeout（e，0）;
  document.addEventListener（“ DOMContentLoaded”，e）
}
（function（e） {
  return new Ie
}
）
}
（）;